import { createStore } from 'vuex';
import countings from './modules/countings';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    countings,
  }
})
